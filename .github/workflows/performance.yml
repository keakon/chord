name: performance

on:
  push:
    branches:
      - "**"
    paths:
      - "internal/tui/**"
      - "internal/llm/**"
      - "internal/session/**"
      - "internal/recovery/**"
      - "internal/agent/**"
      - "scripts/bench_tui_regression.sh"
      - ".github/workflows/performance.yml"
  pull_request:
    paths:
      - "internal/tui/**"
      - "internal/llm/**"
      - "internal/session/**"
      - "internal/recovery/**"
      - "internal/agent/**"
      - "scripts/bench_tui_regression.sh"
      - ".github/workflows/performance.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tui-benchmarks:
    name: Runtime benchmark smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Run runtime benchmark smoke
        env:
          CHORD_BENCH_TIME: 250ms
        run: scripts/bench_tui_regression.sh

---
title: Chord
description: Calm AI coding in your terminal — a lightweight, local-first coding agent with reliable long sessions, hot-swappable model setups, and remote operation.
template: splash
head:
  - tag: title
    content: Chord — Calm AI coding in your terminal
hero:
  tagline: Calm AI coding in your terminal — stable long sessions, model setups you can swap at runtime, and remote operation when you cannot be at the keyboard.
  actions:
    - text: Quickstart
      link: /chord/quickstart/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/keakon/chord
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import screenshot from '../../../../docs/assets/screenshot.png';

<figure class="chord-screenshot">
  <img src={screenshot.src} alt="Chord terminal UI screenshot" />
</figure>

## Three-step setup

<CardGrid>
  <Card title="1. Install" icon="download">
    Prebuilt binaries do not require Go. If you already have Go 1.27.0+ installed:

    ```bash
    go install github.com/keakon/chord/cmd/chord@latest
    ```

    If not, download a prebuilt binary from [GitHub Releases](https://github.com/keakon/chord/releases), extract it, and put `chord` on your `PATH`.
  </Card>
  <Card title="2. Run setup once" icon="pen">
    Run `chord` in an interactive terminal:

    ```bash
    chord
    ```

    If `config.yaml` is missing, Chord launches a one-time setup wizard. The wizard creates the minimal `config.yaml` and, when needed, `auth.yaml`.

    Prefer to write YAML manually or need a different provider/model setup? See [Quickstart](/chord/quickstart/).
  </Card>
  <Card title="3. Run from your project" icon="window">
    ```bash
    cd my-project
    chord
    ```
    On first run in a project, Chord creates `.chord/` as needed.
  </Card>
</CardGrid>

## Why Chord

<CardGrid>
  <Card title="Long sessions use less context" icon="database">
    Chord trims stale tool output at request time, keeps typed summaries for
    large results, and compacts earlier turns before context pressure breaks
    the flow.
  </Card>
  <Card title="Stays out of the way" icon="approve-check-circle">
    Loads sessions with thousands of messages almost instantly, exits without
    a shutdown wait, keeps memory low, and unloads idle LSP/MCP resources.
  </Card>
  <Card title="You see the network state" icon="analytics">
    Precise request status and elapsed wait time during model calls. Never
    wonder if it's stuck again.
  </Card>
  <Card title="Hot-swap model setups" icon="random">
    Group models into reusable pools (`fast`, `thinking`, `cheap`) and switch
    them at runtime via `/models` or `Ctrl+P`. The runtime falls back through
    the ordered list automatically.
  </Card>
</CardGrid>

## More capabilities

<CardGrid>
  <Card title="Keyboard-first, Vim-style" icon="vim">
    Built for keyboard-heavy workflows: Insert / Normal modes, Vim-flavoured
    navigation, and message search.
  </Card>
  <Card title="Project context" icon="magnifier">
    LSP diagnostics, definition/reference/implementation lookups, Git status,
    and `@` file completion.
  </Card>
  <Card title="Images and PDFs" icon="document">
    Paste images, attach image/PDF files when supported, preview images in
    supported terminals, and use `view_image` for local PNG/JPEG files.
  </Card>
  <Card title="Multi-agent collaboration" icon="comment">
    A main agent with focused SubAgents, each with its own context, switchable
    via `Shift+Tab`.
  </Card>
  <Card title="Parallel work via git worktrees" icon="code-branch">
    `chord --worktree feat-auth` spins up an isolated working copy so several
    tasks on the same repo do not stomp on each other.
  </Card>
  <Card title="Import existing sessions" icon="cloud-download">
    Migrate Claude Code, Codex, and OpenCode sessions into resumable Chord
    sessions.
  </Card>
</CardGrid>

## Performance

On a real-world Pebble database task, Chord finished **24% faster** than Codex-CLI while using **63% fewer input tokens** at **63% lower cost**. The app shell starts and exits in **under 1s** and holds an empty session in **31.6MB**.

These are scenario-specific proof points, not universal guarantees. Full tables, methodology, and caveats: [Performance — Measured results](/chord/performance/#measured-results).

## Find docs by task

<CardGrid>
  <Card title="Start quickly" icon="rocket">
    Install, initialize a provider, run `chord doctor models`, and send the first task.
    [Open →](/chord/quickstart/)
  </Card>
  <Card title="Configure models" icon="setting">
    Provider/auth schema plus copy-paste examples for common setups.
    [Config →](/chord/configuration/) · [Examples →](/chord/examples/)
  </Card>
  <Card title="CLI reference" icon="laptop">
    Every command, subcommand, and flag — including `chord cleanup`, worktrees, and headless.
    [Open →](/chord/cli/)
  </Card>
  <Card title="Permissions and safety" icon="padlock">
    The permission model, approval flow, and the security boundaries Chord enforces.
    [Open →](/chord/permissions-and-safety/)
  </Card>
  <Card title="Long tasks" icon="list-format">
    Use `/loop` and `Done` for multi-step implementation work.
    [Open →](/chord/usage/#loop--continuous-execution-mode)
  </Card>
  <Card title="Extend and customize" icon="puzzle">
    Agents, skills, MCP, hooks, and custom slash commands.
    [Customize →](/chord/customization/) · [Hooks →](/chord/hooks/)
  </Card>
  <Card title="Remote control" icon="server">
    Use `chord headless` and chord-gateway safely from another surface.
    [Open →](/chord/headless/)
  </Card>
  <Card title="Troubleshoot" icon="warning">
    Startup, auth, model, TUI, context, and integration symptoms.
    [Open →](/chord/troubleshooting/)
  </Card>
</CardGrid>

{
  "name": "chord-docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Chord user documentation site (Starlight + Astro).",
  "scripts": {
    "sync": "node ../scripts/sync-docs.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro check && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.0",
    "@astrojs/starlight": "^0.30.0",
    "astro": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
